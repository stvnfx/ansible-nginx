server {
        listen 80;

        server_name {{ domains|join(' ') }};
        root {{ app.app_path }}/current/public;
        passenger_enabled on;

        error_page 500 502 503 504 /50x.html;
}